1.  Компилируемый файл - файлы .java - так называемые "Компилированные модули", это файл с исходным текстом на java
    внутри которого может находится только один public класс и сколько угодно default классов.

2. Наследование и композиция позволяют создавать новые типы данных на основе существующих
   при этом композиция применима для возможности использования реализации компонуемого типа в новом типе
   а наследование для повторного использования интерфейса базового класса в новом классе.

3. Восходящее преобразование - Интерпретация ссылки как ссылки на базовый класс

4. Позднее связывание (ПС или dynamic binding) это механизм определяющий реализацию метода для конкретного объекта на этапе
   выполнения. Все методы в java используют механизм ПС, кроме статических методов и методов объявленных
   как final или private (поскольку приват не явно объявлен как финал).

5. Статические методы не поддерживают полиморфного поведения.

6. Конструкторы в java неявно объявлены как static

7. В методах при полиморфизме - типы аргументов не могут быть коваринтными, а возвращаемые значения могут.